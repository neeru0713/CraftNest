import React from 'react'

const Button = ({name}) => {
    return (
          
          <button className="text-white font-bold rounded border border-1 p-2">
            {name}
          </button>
       
     
    );
}

export default Button